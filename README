<interactive-mode>

command    argument	description
=======    ======== 	===========
tables	   N/A     	returns list of tables
describe   <table>	returns table schema
query	   <query>	executes query

query syntax
=====================================================
projection(child; projection-list)
merge-join(left-child, right-child; join-clause; )
sscan(table[:alias]; projection-list; [where-clause])
